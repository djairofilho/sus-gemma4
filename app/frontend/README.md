# Frontend

Vite + React web shell for Gemma SUS Assistant.

## Development

```bash
npm install
npm run dev
```

The frontend expects the local backend at `http://localhost:8000` by default. Override with:

```bash
VITE_API_BASE_URL=http://localhost:8000 npm run dev
```
